name: PHPUnit Tests

on:
  push:
    branches: [ testci ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup PHP Action
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
          coverage: xdebug

      - name: Validate composer.json and composer.lock
        run: composer validate src/composer.json

      - name: Install dependencies
        run: composer install src/composer.json --no-progress --prefer-dist --optimize-autoloader 

      - name: Run PHPUnit
        run: src/vendor/bin/phpunit --coverage-text
